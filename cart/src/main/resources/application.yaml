server:
  port: 8081

spring:
  application:
    name: Cart
  data:
    mongodb:
      host: localhost
      port: 27017
      database: multikart_cart

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka